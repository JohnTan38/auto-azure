/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['iili.io'],
    unoptimized: true,
  },
  // Add this line output: 'export',
};

module.exports = nextConfig
